# Patterns Directory - DEPRECATED

This directory has been reorganized. All pattern files have been moved to:

```
/data/
├── generated-patterns/    # Auto-generated libraries
├── example-patterns/      # Learning templates
└── demo-patterns/         # Hand-crafted demos
```

See `/data/README.md` for the new structure.

This directory is kept for backward compatibility and may be removed in future versions.
